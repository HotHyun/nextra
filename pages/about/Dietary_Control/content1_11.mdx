# 식단일기 작성법 예시

식단일기는 자신이 어떤 생활을 하는지 객관적으로 보여주고, 이를 구체화합니다. 열량을 세세히 적지 않더라도 무엇을 먹고, 어떻게 생활하는지 구체화하면 많은 분이 놀라리라 믿습니다. www.neonb.net 에 가시면 빈 노트를 내려받을 수 있습니다. 한번 출력해서 사용해보시면 어떨까요?

import PageKey from "../../../components/PageKey";

import { useRouter } from "next/router";

export const Final = () => {
  const router = useRouter();
  return <PageKey route={router.pathname} />;
};

<Final />
