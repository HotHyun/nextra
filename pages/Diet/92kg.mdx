# 92kg

import Button from "../../components/Button";
import { useEffect } from "react";
import mixpanel from "mixpanel-browser";

export const NineTwo = () => {
  useEffect(() => {
    const timer = setTimeout(() => {
      mixpanel.track("View Web Interactive Content Page", {
        contentId: "Nine Two",
      });
    }, 1000);
  }, []);
  return (
    <div className="lg:flex">
      <div className="lg:mr-2 lg:w-1/3 w-full">
        <Button
          title="Make Environment"
          description="환경을 만들자!"
          query="/Diet/92kg"
        ></Button>
      </div>
      <div className="lg:ml-2 lg:mr-2 lg:w-1/3 w-full">
        <Button
          title="Eating Style"
          description="밥 먹는 습관부터!"
          query="/Diet/92kg"
        ></Button>
      </div>
      <div className="lg:ml-2 lg:w-1/3 w-full"></div>
    </div>
  );
};

<NineTwo />
