# 84kg

import Button from "../../components/Button";
import { useEffect } from "react";
import mixpanel from "mixpanel-browser";

export const Eight_Four = () => {
  useEffect(() => {
    const timer = setTimeout(() => {
      mixpanel.track("View Web Interactive Content Page", {
        contentId: "Eight Four",
      });
    }, 1000);
  }, []);
  return (
    <div className="lg:flex">
      <div className="lg:mr-2 lg:w-1/3 w-full">
        <Button title="gym" description="헬스장에 가볼까?" query="/Diet/84kg" />
      </div>
      <div className="lg:ml-2 lg:mr-2 lg:w-1/3 w-full">
        <Button
          title="YoYo Phenomenon"
          description="요요 현상"
          query="/Diet/84kg"
        ></Button>
      </div>
      <div className="lg:ml-2 lg:w-1/3 w-full"></div>
    </div>
  );
};

<Eight_Four/>
